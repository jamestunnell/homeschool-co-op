<%= f.association :section, :label_method => lambda {|x| x.course.title } %>
<%= f.input :student_id, as: :hidden %>
<%= f.button :submit, :class => 'btn btn-primary' %>
<%= link_to "Cancel", student_enrollments_path(@enrollment.student), :class => 'btn btn-default' %>