<%= simple_form_for [@room.building,@room], :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= f.association :building, as: :hidden %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to "Cancel", @room.building, :class => 'btn btn-default' %>
<% end %>
