<%= simple_form_for @room, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= f.association :building, include_blank: false %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to "Cancel", rooms_path, :class => 'btn btn-default' %>
<% end %>
