<%= simple_form_for(@adult, html: { class: 'form-horizontal' }) do |f| %>
  <%= f.input :first %>
  <%= f.input :last %>
  <%= f.input :email %>
  <%= f.button :submit, :class => 'btn btn-primary' %>
  <%= link_to "Cancel", children_path, :class => 'btn btn-default' %>
<% end %>
